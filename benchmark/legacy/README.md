# Legacy benchmarks

`pr1/` and `pr2/` are the agents from this repository's PR #1 and PR #2. They
finish around $13k-$35k a game, against ~$75k-$130k for ladder-level agents. They
are kept only as default opponents for the tuning tools of `agents/scratch/`.
